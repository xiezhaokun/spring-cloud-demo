spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8765/
      profile: dev
      label: master
  rabbitmq:
    addresses:
    username:
    password:

server:
  port: 8766
